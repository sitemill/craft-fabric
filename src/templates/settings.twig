{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Library plugin for Craft CMS 3.x
 *
 * Library Settings.twig
 *
 * @author    SiteMill
 * @copyright Copyright (c) 2020 SiteMill
 * @link      https://sitemill.co
 * @package   Library
 * @since     1.0.0
 */
#}

{% import "_includes/forms" as forms %}

{#{% do view.registerAssetBundle("sitemill\\library\\assetbundles\\library\\LibraryAssets") %}#}

{{ forms.textField({
    label: 'LibraryAssets volume handle',
    instructions: 'The handle of the asset volume that will power Library.',
    id: 'assetsHandle',
    name: 'assetsHandle',
    value: settings['assetsHandle']})
}}

{{ forms.textField({
    label: 'Lightbox section handle',
    instructions: 'The handle of the lightboxes section.',
    id: 'lightboxesHandle',
    name: 'lightboxesHandle',
    value: settings['lightboxesHandle']})
}}

{{ forms.textField({
    label: 'Pages section handle',
    instructions: 'The handle of the pages section.',
    id: 'pagesHandle',
    name: 'pagesHandle',
    value: settings['pagesHandle']})
}}

{{ forms.textField({
    label: 'Categories group handle',
    instructions: 'The handle of the category group.',
    id: 'categoriesHandle',
    name: 'categoriesHandle',
    value: settings['categoriesHandle']})
}}
{# @var craft \craft\web\twig\variables\CraftVariable #}
{#
/**
 * Library plugin for Craft CMS 3.x
 *
 * Library Settings.twig
 *
 * @author    SiteMill
 * @copyright Copyright (c) 2020 SiteMill
 * @link      https://sitemill.co
 * @package   Library
 * @since     1.0.0
 */
#}

{% import "_includes/forms" as forms %}

{% do view.registerAssetBundle("sitemill\\library\\assetbundles\\library\\LibraryAsset") %}

{{ forms.textField({
    label: 'Some Field',
    instructions: 'Enter some setting here.',
    id: 'someAttribute',
    name: 'someAttribute',
    value: settings['someAttribute']})
}}
