<div class="listing-context">
    Listing items
    {% if category %} in '{{ category.title }}'{% endif %}, sorted by
    {% switch order %}
    {% case 'title asc' %}
        Title ascending
    {% case 'title desc' %}
        Title descending
    {% endswitch %}
</div>

 