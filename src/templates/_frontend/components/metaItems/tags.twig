<div class="meta-item">
    <h4 class="meta-item__title">
        {{ field.name }}
    </h4>
    <div class="meta-item__value">
        {% for tag in craft.tags.groupId(field.group.id).relatedTo(asset.id).all() %}
            {{ tag }}
        {% endfor %}
    </div>
</div>