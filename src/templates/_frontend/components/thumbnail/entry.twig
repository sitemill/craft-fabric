{% extends '_fabric/components/thumbnail' %}

{% block thumbnail %}
    {% if thumbnail|length %}
        <img class="absolute w-full h-full object-contain p-4" src="{{ thumbnail.url({width:400}) }}" alt="{{ alt }}">
    {% else %}
        {{ parent() }}
    {% endif %}
{% endblock %}
