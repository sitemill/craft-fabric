{% macro css(handle,color,parent = null) %}

{#    .bg-{{ parent ? parent ~ '-' }}{{ handle }}-50 {#}
{#        background-color: {{ color }};#}
{#    }#}
{% endmacro %}

{#{% macro textCss(handle,color,parent = null) %}#}
{#    .{{ parent ? parent ~ '-' }}{{ handle }} {#}
{#    color: {{ color }};#}
{#    }#}
{#{% endmacro %}#}

{#{% macro colorLoop(items,parent) %}#}
{#    {% for handle, color in items %}#}
{#                {{ _self.css(handle,color,parent) }}#}
{#    {% endfor %}#}
{#{% endmacro %}#}

{% css %}
.text-primary { color: {{ craft.fabric.brandColor }}; }
.bg-primary { background-color: {{ craft.fabric.brandColor }}; }
.border-primary {border-color: {{ craft.fabric.brandColor }}; }
{#.focus\:border-{{ parent ? parent ~ '-' }}{{ handle }}:focus { border-color: {{ color }}!important;}#}
{% endcss %}